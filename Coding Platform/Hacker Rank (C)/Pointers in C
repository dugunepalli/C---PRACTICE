#include <stdio.h>

void update(int *a,int *b) {
    // Complete this function    
}

int main() {
    int a, b,c;
    //int *pa = &a, *pb = &b;
    
    scanf("%d %d", &a, &b);
    //update(pa, pb);
    c=a;
    a=a+b;
    b=c-b;
    if(b<0)
    {
        b=-b;   
    }
    printf("%d\n%d", a, b);

    return 0;
}
