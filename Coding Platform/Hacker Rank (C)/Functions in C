#include <stdio.h>
/*
Add `int max_of_four(int a, int b, int c, int d)` here.
*/

int main() {
    int a, ans=0;
    for(int i=0;i<4;i++)
    {
        scanf("%d", &a);
        if(a>=ans)
        {
            ans = a;
        }
    }
    printf("%d",ans);
    return 0;
}
