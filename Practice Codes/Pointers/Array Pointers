#include<stdio.h>
int main()
{
    //Array POinters
    int arr[5]={1,2,3,4,5};
    for(int i=0;i<5;i++)
    {
        printf("Address of %d is %p and Value is: %d\n", i, &arr[i], arr[i]);
    }
    return 0;
}
